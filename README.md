# Toxic_classification_and_FastAPI
API for social platform with ML content moderation. Users register, create posts and comments. Toxicity detection checks all content before publishing, blocks if toxic. Built with FastAPI, PostgreSQL, SQLAlchemy, Alembic. JWT auth with bcrypt. Full CRUD for posts and comments.
